<% @posts.each do |post| %>
<h1 class="judul"><span><%= link_to post.title, post_path(post.id) %></span></h1>
<div class="content"><%= post.content %> 
<%= link_to "Baca Selengkapnya", post_path(post.id) %> </div>
<% end %>